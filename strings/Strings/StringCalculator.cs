using System;
using System.Collections.Generic;

namespace Strings
{
    public class StringCalculator
    {
        public int Add(string numbers)
        {
            int result=0;

            if (numbers.Length == 0)
                return result;
            else
                return result;
        }
    }
}